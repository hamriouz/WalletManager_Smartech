server:
    port: 9040
spring:
    datasource:
        url: jdbc:h2:mem:demo;DB_CLOSE_ON_EXIT=FALSE
    elasticsearch:
        uris: localhost:9200
    jpa:
        hibernate:
            ddl-auto: create
    kafka:
        bootstrap-servers: localhost:19092
        consumer:
            group-id: myGroup
